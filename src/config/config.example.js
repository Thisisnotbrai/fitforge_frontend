/**
 * Example configuration file
 *
 * INSTRUCTIONS:
 * 1. Make a copy of this file and name it "config.js"
 * 2. Replace the API key with your actual key in config.js
 * 3. Keep this example file in git but add config.js to .gitignore
 */

export const config = {
  openRouter: {
    apiKey: "YOUR_API_KEY_HERE",
    endpoint: "https://openrouter.ai/api/v1/chat/completions",
  },
};

export default config;
